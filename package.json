{
  "name": "@guardian/grid-client",
  "version": "1.0.2",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "lint": "tslint -p tsconfig.json",
    "format": "prettier --write \"src/**/*.{ts,js}\"",
    "test": "jest",
    "test:dev": "jest --watch",
    "build": "rm -rf lib && tsc -p tsconfig.json",
    "build:dev": "tsc-watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.5",
    "jest": "^26.1.0",
    "prettier": "^2.0.5",
    "ts-jest": "^26.1.3",
    "tsc-watch": "^4.2.9",
    "tslint": "^6.1.2",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.9.7"
  },
  "files": [
    "lib/**/*"
  ],
  "publishConfig": {
    "access": "public"
  }
}
